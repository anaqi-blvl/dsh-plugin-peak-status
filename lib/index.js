/**
 * Node half of dsh-peak-status. Everything this plugin does happens in the
 * browser (a sidebar footer widget + modal); the host half is a no-op cordis
 * plugin whose only job is to be a loadable entry so the client half joins
 * the web roster.
 */
export default function () {
  /* intentionally empty */
}
